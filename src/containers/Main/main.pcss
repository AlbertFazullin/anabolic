@import "normalize.css";

html,
body {
  height: 100%;
}

a {
  outline: none;
  text-decoration: none;

  &:visited {
    color: #fcfbfb;
  }
}

body {
  background-color: rgba(0, 0, 0, .85);

  font-family: Khula;
  color: #fffefe;
}
